enum SizeProperty { width, height }

enum BlocState {
  initial,
  loading,
  success,
  error,
}

enum Sign {
  signUp,
  signIn,
  forgotPassword,
  resetPassword,
}

enum Statistic {
  comment,
  like,
}

enum AppBarLeading {
  add,
  back,
}

enum Comments {
  my,
  liked,
}
